Hw_1_1:用C/C++或MATLAB实现NxN的DFT），将两幅灰度图像的幅度和相位信息进行置换，比较置换前后差别；
其中调用两个函数分别为mydft2,myidft2，分别是离散傅里叶变换及其逆变换。其中两个函数是通过dft矩阵求解；
具体模块作用写在代码注释中。

Hw_2_2:用C/C++或MATLAB实现8x8块大小的2D-DCT变换，分别保留前5，前10，前20个DCT变换系数，然后计算重构图像与原始图像之间的PSNR，并绘图;
其中调用两个函数分别为mydct2,myidct2，分别是离散余弦变换及其逆变换。这两个函数通过dct矩阵求解，其中mydct2中可以保留前一定数量的DCT变换系数；
具体模块作用写在代码注释中。